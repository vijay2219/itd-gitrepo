apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
spec:
  replicas: 2
  selector:
    app: nginx
  template:
    metadata:
      app: nginx
    spec:
      containers:
        - name: my-nginx
          image: nginx
          ports:
           - containerPort: 80